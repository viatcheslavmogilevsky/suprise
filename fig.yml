play:
  image: ansible/ubuntu14.04-ansible:stable
  volumes:
    - "./symlinks/to-docker:/bin/docker"
    - "./symlinks/to-fig:/bin/fig"
    - "/var/run/docker.sock:/run/docker.sock"
    - "./extra:/suprise"
    - "/figdir"
  working_dir: /suprise
  entrypoint: ansible-playbook
